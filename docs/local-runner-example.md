# Local Runner Example

This file was created as part of a synthetic Local Runner v0 dry run
(task ID `SM-CC-EXAMPLE-003`). It does not document any real feature,
behavior, or system in this repository — it exists only to verify that
the Local Runner can inspect the repository, make a scoped file change,
and hand off to Git/PR automation under Owner-approved boundaries.

No application code, dependencies, migrations, authentication, or
Production settings were changed as part of this task.
